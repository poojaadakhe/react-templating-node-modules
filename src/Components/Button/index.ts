/**
 * If we don't write below statement, 
 * User will have to import Button like this:-
 * import Button from './Button/Button'; 
 * 
 * which doesn't make sense. After below change, it will be:-
 * 
 * import Button from './Button'; 
 */
export {default as Button } from './Button';